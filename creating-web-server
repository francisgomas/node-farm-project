const fs = require('fs');
const http = require('http');

//create server
const server = http.createServer((req, res) => {
    res.end('Hello from the server!');
});

//listening from the created server
server.listen(8000, '127.0.0.1', () => {
    console.log('Listening on port 8000!');
});